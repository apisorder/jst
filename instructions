#	install the Node Package Manager
sudo apt install npm
#	which is used to install Yarn
sudo npm install -g yarn
#	add the necessary node modules (libraries, etc.)
sudo npm install
#	in some cases, the project can't start, so update NPM as a preventative measure
sudo npm update
#	run the project
sudo yarn start

#	in some cases, it's set, instead of export
export NODE_OPTIONS=--openssl-legacy-provider && yarn start
